# i18n and copy overrides

ConversaCore supports:

- locale
- fallbackLocale
- per-message overrides

Use company layer for tone-of-voice and domain wording.

Use core i18n for reusable UI labels and action copy.

Recommended strategy:

- keep generic keys in core
- inject company language packs at runtime